# SolarClean 工程功能报告

## 项目信息

- 项目名称：SolarClean 光伏清洗载板固件
- 主控平台：STM32H743VITx
- 实时系统：FreeRTOS
- DJI 接入：DJI Payload SDK
- 客户升级工具：`serial_ota/SolarCleanFirmwareUpdater.exe`

## 当前工程能力

1. DJI PSDK 连接与控件交互。
2. 4G/Air780E MQTT 通信。
3. 水泵、舵机和清洗机构控制。
4. 航线下载、存储、删除和执行相关接口。
5. Bootloader + 应用 A 区 + 升级暂存 B 区 + 航线存储区 + 升级状态区。
6. Type-C/CP210x 虚拟串口固件升级。
7. Windows 客户升级工具，支持选择固件、连接设备、进度显示和升级结果提示。

## Flash 分区

| 区域 | 地址范围 | 大小 | 说明 |
| --- | --- | ---: | --- |
| 引导程序 | `0x08000000 - 0x0801FFFF` | 128KB | 上电启动、升级安装 |
| 应用程序 A 区 | `0x08020000 - 0x080BFFFF` | 640KB | 正式运行固件 |
| 升级暂存区 B | `0x080C0000 - 0x0815FFFF` | 640KB | 串口/4G 升级暂存固件 |
| 航线存储区 | `0x08160000 - 0x081DFFFF` | 512KB | 航线 KMZ/JSON 等任务文件 |
| 升级状态区 | `0x081E0000 - 0x081FFFFF` | 128KB | 保存升级状态和校验信息 |

所有产品形态统一使用该布局。无航线版本保留航线存储区不用，避免跨版本 OTA 时 App 写入 B 区地址和 Bootloader 读取 B 区地址不一致。

## SRAM 使用

STM32H743 片内 SRAM 总量为 1MB。当前链接脚本给应用程序使用：

- DTCM：128KB，`0x20000000 - 0x2001FFFF`
- AXI SRAM：512KB，`0x24000000 - 0x2407FFFF`

## 升级流程

1. 设备上电后，Bootloader 判断升级状态。
2. 正常情况下跳转应用程序 A 区运行。
3. 串口或 4G OTA 将新固件写入 B 区。
4. 应用程序校验 SHA256，通过后写入待升级状态并复位。
5. Bootloader 校验 B 区固件，并安装到 A 区。
6. 新固件启动后确认升级状态。

## 版本迁移说明

从旧布局迁移到统一布局时，需要现场或生产烧录一次 Bootloader 和 App：

- Bootloader 烧录地址：`0x08000000`
- App 烧录地址：`0x08020000`
- 擦除 OTA 状态区：`0x081E0000 - 0x081FFFFF`

完成迁移后，后续串口 OTA 和 4G OTA 都使用 B 区 `0x080C0000 - 0x0815FFFF`。

## 上传注意

SVN 上传前不要提交 Keil 中间产物、临时缓存、私有 DJI App Key、License、MQTT 密码等信息。实机编译前需要在本地恢复私有配置。
